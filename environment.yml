name: 3dmolgen
channels:
  - conda-forge
  - nvidia
  - pytorch
  - defaults
dependencies:
  - python=3.10
  - pytorch>=2.5.0
  - pytorch-cuda=12.1
  - rdkit
  - pyyaml
  - pip
  - pip:
    # Core dependencies
    - numpy>=1.17.0
    - transformers>=4.30.0
    - tokenizers>=0.13.0
    - safetensors>=0.3.0
    - tqdm>=4.60.0
    - loguru>=0.7.0
    - tyro>=0.5.0
    - tomli>=2.0.0
    - datasets>=2.0.0
    - fsspec>=2023.0.0
    - filelock>=3.0.0
    - packaging>=20.0
    - typing-extensions>=4.0.0
    # Optional: Pretraining
    - torchtitan>=0.1.0
    - torchdata>=0.8.0
    - accelerate>=1.0.0
    - flash-attn>=2.0.0
    - cloudpickle>=2.0.0
    - submitit>=0.20.0
    # Optional: GRPO
    - trl>=0.7.0
    # Optional: Evaluation
    - posebusters>=0.4.0
    - scikit-learn>=1.0.0
    - scipy>=1.7.0
    # Optional: Tracking
    - wandb>=0.15.0
    - tensorboard>=2.10.0
    # Development
    - pytest>=7.0.0
    - black>=23.0.0
    - flake8>=6.0.0
    - mypy>=1.0.0

